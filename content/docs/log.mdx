---
title: "log"
---

# 운영 로그

이 파일은 Karpathy LLM Wiki의 append-only `log.md` 역할을 합니다. 시간순으로 **ingest · query · lint**만 기록합니다. 현재 지식의 위치는 [[index|중앙 목차]]에서 찾습니다.

## 기록 규칙

- **ingest**: 새 원본을 읽고 어떤 판단·페이지·링크를 갱신했는지 남깁니다.
- **query**: 질문이나 검색이 새 비교·판단·페이지 수정·후속 조사를 만들었을 때만 남깁니다.
- **lint**: 모순, 오래된 주장, 고립 문서, 누락 연결, 데이터 공백을 점검한 결과를 남깁니다.
- 단순 조회·같은 검색의 반복은 기록하지 않습니다.

각 query 기록에는 질문, 검색어, 읽은 Agent Wiki 페이지, raw KL을 사용했다면 경로와 ingest 상태, 결과 페이지 또는 결정만 짧게 남깁니다.

## 2026-09-10 19:30 UTC | 타오르미나 배경 관찰 diary 색인

- ingest: 최근 source 중 `AI_RESEARCH_BRIEFING.md`, `EVALUATION_NOTES.md`, `OPENCLAW_RUNTIME_MAINTENANCE.md`, `LOCAL_REVIEW_AUTOMATION.md`, `daily-reviews/2026-09-09-review.md`, `wiki-retrieval-test-20q-02.md`, `version_trackers.yaml`을 후보로 점검했다.
- query: 운영 규칙은 기존 maintenance/insights 문서와 중복되는지, daily review는 반복 가능한 공간 판단으로 남길 가치가 있는지 확인했다.
- 읽은 문서: [[index|중앙 목차]], [[maintenance/version-tracker-refresh|버전 트래커 갱신 원칙]], [[maintenance/review-routing-currentness|현재성 점검]], [[insights/infinity-archive-knowledge-promotion|실행 원장에서 지식으로 승격하기]], [[diary/2026-08-30|최근 일일 인덱스]].
- raw KL: `source/openclaw-system/data/daily-reviews/2026-09-09-review.md`는 queued 원본으로, 건물과 배경의 관계를 공간 판단 기준으로 삼는 관찰만 얇게 색인했다.
- 결과: [[diary/2026-09-09|2026-09-09 일일 인덱스]]를 추가했다. AI research briefing, 평가 기준, runtime maintenance, local review automation, Infinity 회수성 재검증, 버전 트래커 최신값은 기존 운영 문서와 중복되거나 실행 설정에 가까워 별도 승격하지 않았다.

## 2026-09-10 12:20 UTC | 미니멀 모으기 시장·포맷 경쟁 재검토

- query: 한국의 `N/1000 모으기` 연재 포맷이 실제로 쓰이는지, 미니멀 수집 아카이브의 직접 경쟁과 대체재가 무엇인지, 수집이 아카이브/참여로 커진 글로벌 선례가 있는지 재검토했다.
- 검색어: `site:youtube.com "1000개" "모으기"`, `"미니멀 라이프" 커뮤니티 회원수`, `"100 Things Challenge"`, `"Things Organized Neatly" archive`, `The 100 Day Project`.
- 읽은 문서: [[index|중앙 목차]], [[insights/structure-before-scale|확장 전에 구조를 본다]], [[insights/context-over-inventory|맥락이 목록보다 중요하다]], `DOCUMENT_SEARCH_PIPELINE.md`, `APP_WEB_RESEARCH_STATS_SOURCES.md`.
- 외부 근거: YouTube 검색 결과에서 `내가 좋아하는 밴드노래 브릿지 1000개 모으기`가 최소 46/1000까지 번호로 연재된 사례를 확인했다. `The 100 Day Project`는 매일의 공개 기록, 고유 해시태그, 상호 발견을 결합한다. `Things Organized Neatly`는 사진 수천 장을 축적한 수집형 아카이브이며, 창작자 제보를 받았다. `100 Things Challenge`는 보유물 100개 제한이라는 참여형 미니멀 챌린지다.
- 결과: 국내의 검증된 직접 경쟁은 대형 일반 미니멀 커뮤니티이고, `미니멀 사례 N/1000 → 제보 선별 → 탐색 아카이브`의 동일한 결합은 이번 공개 검색에서 확인하지 못했다. 따라서 차별화의 단위는 미니멀 일반론이 아니라 사례의 판정 기준·발견 카드·출처/제보 루프다. 조회수만으로 포맷 유행이나 수요 규모를 확정하지 않으며, 초기 30편에서 완주율·저장·제보·재방문 의향을 따로 확인한다.

## 2026-09-10 11:55 UTC | 미니멀 사례 수집형 콘텐츠의 선례 점검

- query: 한 주제의 사례를 100~1,000개까지 연재·축적한 뒤 참여형 아카이브/커뮤니티로 전환하는 `미니멀 모으기` 모델에 국내 직접 선례가 있는지, 숫자 기반 수집·챌린지의 인접 선례는 무엇인지 점검했다.
- 검색어: `1000개 모으기 콘텐츠`, `100개 모으기 인스타그램`, `1000개 프로젝트 SNS 한국`, `미니멀리즘 커뮤니티 한국`, `Project 333`, `100 Thing Challenge`.
- 읽은 문서: [[index|중앙 목차]], [[insights/structure-before-scale|확장 전에 구조를 본다]], [[insights/context-over-inventory|맥락이 목록보다 중요하다]], `DOCUMENT_SEARCH_PIPELINE.md`.
- 외부 근거: 국내 네이버 `미니멀 라이프` 커뮤니티는 2021년 기준 24.5만 회원으로 언급된 자료를 확인했다. Project 333은 33개 이하의 의류로 3개월을 지내는 미니멀 패션 챌린지와 커뮤니티를 운영한다. 검색 가능한 공개 근거 범위에서는 국내의 `미니멀 사례 100/1000` 카운트형 연재에서 참여형 아카이브로 전환한 직접 선례는 확인하지 못했다.
- 결과: 수집형 계정 자체보다 `항목의 포함 기준`, `번호/누적 진척`, `제보→선별→재게시` 루프가 제품의 차별점이다. 첫 검증은 사이트 구축 전 30개 수집과 제보 전환율을 측정하는 것이 적합하다.

## 2026-09-10 11:32 UTC | 미니멀 수집 1/100의 검증 경계

- query: `미니멀 모으기 1/100`과 AI 고수·창작자 수집, 여행 100일 후기, 커뮤니티·AI 솔루션·영어권·오프라인 확장 아이디어를 한 제품으로 섞지 않고 검증 가능한 첫 시리즈로 좁힐 수 있는지 검토했다.
- 검색어: `title selection pressure`, `evidence bounded content experiment`, `original proof distribution`, `onboarding gates`, `bounded experiment loop`, `100 Day Project`, `Ship 30 for 30`, `Duolingo streak`.
- 읽은 문서: [[index|중앙 목차]], [[insights/title-selection-pressure|제목은 장소가 아니라 선택 압력을 판다]], [[insights/evidence-bounded-content-experiment|증거가 있는 콘텐츠 실험]], [[insights/original-proof-distribution-loop|원본 증명에서 배포로]], [[insights/first-session-onboarding-gates|첫 세션 온보딩 게이트]], [[insights/bounded-experiment-loop|경계가 있는 반복 개선 루프]].
- raw KL: 사용하지 않음. 외부 사례는 공식 운영 페이지와 공식 제품 블로그만 사용했으며, 시장 적합의 직접 증거로 과장하지 않았다.
- 결과: 첫 실험은 `미니멀 모으기 1/100` 한 시리즈의 반응을 검증하는 데만 둔다. AI 고수·창작자 수집은 이후 소재 확장 후보, 커뮤니티·레벨·영문·오프라인·AI 솔루션은 반복 반응이 생긴 뒤의 별도 가설로 분리했다. 실행 전 계획은 Infinity `research-minimal-collector-validation-20260910`에 등록했다.

## 2026-09-09 19:30 UTC | 콘텐츠 기준 이미지 루프 승격

- ingest: 최근 source 중 `daily-review-drafts/2026-09-08-review.md`, `daily-review-drafts/2026-09-09-review.md`, `version_trackers.yaml`를 후보로 점검했다.
- query: 콘텐츠 제작 반복 규칙, proposer 운영 루프, 버전 트래커 최신값 판단이 기존 위키와 중복되는지 확인했다.
- 읽은 문서: [[index|중앙 목차]], [[insights/single-scene-single-criterion-carousel-contract|한 장면은 한 판단만 담는다]], [[maintenance/version-tracker-refresh|버전 트래커 갱신 원칙]], [[insights/infinity-archive-knowledge-promotion|실행 원장에서 지식으로 승격하기]].
- raw KL: `daily-review-drafts/2026-09-08-review.md`는 queued 원본으로, 한 장면을 기준 이미지와 프롬프트로 고정한 뒤 변주한다는 반복 제작 판단만 압축했다.
- 결과: 기존 캐러셀 문서에 기준 이미지 기반 반복 제작 루프를 추가했다. 2026-09-09 회고의 proposer 점검은 2026-09-08 운영 로그와 중복되어 보류했고, 버전 트래커 최신값 변경은 기존 maintenance 문서 원칙으로 충분해 별도 승격하지 않았다.

## 2026-09-08 07:43 UTC | Observe → Intent → Act → Learn 운영 루프 확장

- query: OpenClaw의 선제성은 새 신호를 더 많이 실행하는 데 있지 않고, 운영 교훈을 안전한 개선 후보로 순환시키는 데 있다는 요구를 반영했다.
- 검색어: `Observe`, `Intent`, `permission_level`, `Operating Lessons`, `execution learning`.
- 읽은 문서: [[index|중앙 목차]], `SIGNAL_TO_INTENT_PROPOSER.md`, `INFINITY_OPERATING_RULES.md`, `EXECUTION_LEARNING_CONTRACT.md`, OpenClaw·Infinity `OPERATING_LESSONS.md`, `MARKETING_LEARNINGS.md`.
- 결과: 변경 감지 대상에 운영 교훈 원장을 추가했다. 실행은 기존 Infinity dispatcher와 권한 게이트만 담당하며, 반복 교훈은 근거·효과·검증 기준이 갖춰진 경우에만 승인 대기 개선 intent가 된다.

## 2026-09-07 21:40 UTC | 자율 제안 루프의 안전한 재가동

- query: OpenClaw가 새 신호를 선제적으로 일감으로 전환하되, 독단적인 실행과 반복 알림을 피하는 운영 경계를 점검했다.
- 검색어: `signal-to-intent`, `proposer`, `approval`, `heartbeat`, `Infinity`.
- 읽은 문서: [[index|중앙 목차]], `DOCUMENT_SEARCH_PIPELINE.md`, `SIGNAL_TO_INTENT_PROPOSER.md`, `EVALUATION_NOTES.md`, `QUERY_GAPS.md`.
- raw KL: `EVALUATION_NOTES.md`와 `QUERY_GAPS.md`를 현재 신호 후보 확인에만 사용했다. 둘은 운영 정본이며 제안의 근거 경로로만 남긴다.
- 결과: proposer를 30분 변경 감지 기반·Inbox 제안 전용으로 바꿨다. 모든 작업은 사용자 승인 전 Active로 승격하거나 실행하지 않으며, Infinity 독립 저장소의 `origin/main`만 완료 조건으로 확인한다.

## 2026-09-07 19:30 UTC | Infinity 승격 흐름과 마감 기록 보강

- ingest: `source/openclaw-system/docs/INFINITY_OPERATING_RULES.md`의 최근 갱신분에서 승격 결과를 추적하는 `knowledge-flow` receipt와 terminal report 규칙을 확인했다.
- query: 기존 `실행 원장에서 지식으로 승격하기` 문서가 Archive 판정과 `knowledge_targets` 검증까지 다루는지, 별도의 흐름/마감 추적 규칙이 필요한지 검토했다.
- 읽은 문서: [[index|중앙 목차]], `INFINITY_OPERATING_RULES.md`, [[insights/infinity-archive-knowledge-promotion|실행 원장에서 지식으로 승격하기]].
- 결과: 같은 페이지에 흐름 증빙과 마감 기록을 덧붙여, 승격 판단이 어디서 왔고 어디까지 닫혔는지 다시 찾을 수 있게 보강했다. 중복되는 Archive 개념은 새 페이지로 분리하지 않았다.

## 2026-09-06 19:30 UTC | source/openclaw-system 전체 점검과 캐러셀 편집 규칙 승격

- ingest: `source/openclaw-system`의 최근 변경분과 `INSTAGRAM_CAROUSEL_PROMPT_SYSTEM.md`, `INFINITY_OPERATING_RULES.md`, `LOCAL_REVIEW_AUTOMATION.md`를 점검했다.
- query: 캐러셀 편집 계약, 공개 전 안전 게이트, 현재성 라우팅이 기존 위키의 `title-selection-pressure`, `review-routing-currentness`, `currentness-safe-travel-context`와 어떻게 겹치는지 확인했다.
- 읽은 문서: [[index|중앙 목차]], `DOCUMENT_SEARCH_PIPELINE.md`, `INSTAGRAM_CAROUSEL_PROMPT_SYSTEM.md`, `INFINITY_OPERATING_RULES.md`, `LOCAL_REVIEW_AUTOMATION.md`.
- 결과: [[insights/single-scene-single-criterion-carousel-contract|한 장면은 한 판단만 담는다]]를 추가해, 한 장면-한 판단-증거-공개 게이트를 재사용 규칙으로 압축했다. 나머지 운영 원칙은 기존 위키와 충분히 겹쳐 중복 승격하지 않았다.

## 2026-09-02 09:20 UTC | 사용자 정본 노트 2026-09 동기화

- ingest: `shdkej.github.io/content`의 최신 커밋 `ccaa743`를 받아 `source/shdkej-content/`에 동기화했다. 변경 원본 30개와 새 `2026-09.md`, `Meta/BRANDING.md`를 ingest manifest에 반영했다.
- query: `AI`, `Travel`, `BRANDING`, `source↔mapped`, `8×8`을 기준으로 새 원본이 기존 Agent Wiki의 탐색 계약을 깨는지 확인했다.
- 읽은 문서: [[index|중앙 목차]], [[mapped/deep-knowledge/ai|AI]], [[mapped/Idea/Travel|Travel]], [[mapped/Integration/Marketing|Marketing]], [[mapped/source-category-map|원본 카테고리 지도]].
- raw KL: `source/shdkej-content/Deep Knowledge/AI.md`, `Idea/Travel.md`, `Meta/BRANDING.md`, `2026-09.md`를 사용했다. 모두 이번 동기화 뒤 ingest manifest에 등록된 원본이며, 장기 판단은 이 페이지에만 압축했다.
- 결과: [[insights/2026-09-source-refresh|2026-09 정본 노트 갱신]]과 [[mapped/Meta/BRANDING|브랜딩]]을 추가했다. `BRANDING`은 원본 Meta 축의 새 노드이므로 1:1 대응으로 편입했고, 8개 축·63개 노드 inventory를 생성·검증했다.

## 2026-09-02 08:10 UTC | 20문항 Agent Wiki 회수성 검증

- query: AI/기술·아키텍처/MSA·미니멀 라이프·보안/비즈니스의 20개 질문에 대해, Agent Wiki만으로 답·정확 locator·첫 도달 경로를 재현할 수 있는지 검증.
- 검색어: `어시스턴트`, `GPT-5`, `G.S.T.A.R`, `back propagation`, `PyPy`, `policy/detail`, `CQRS`, `Netflix junk food`, `Kubernetes abstraction`, `심플/휑`, `만다라트`, `Work Log`, `HTTPS`, `GitHub SSH`, `GS그룹`, `도서정가제`, `Carrying Capacity`.
- 읽은 문서: [[mapped/deep-knowledge/ai|AI]], [[mapped/Fundamental/Architecture|Architecture]], [[mapped/deep-knowledge/data|Data]], [[mapped/deep-knowledge/container|Container]], [[mapped/Human/Balance|Balance]], [[mapped/Communication/Blogging|Blogging]], [[mapped/Integration/Work|Work]], [[mapped/Integration/Business|Business]], [[mapped/deep-knowledge/product|Product]], [[insights/structure-before-scale|구조를 먼저 만드는 법]].
- raw KL: 사용하지 않음. `agent-wiki/content/docs/`만 검색·근거로 사용.
- 결과: Found 6/20, Partial 6/20, Not found 8/20. 8×8 축 구조보다 고유명사·수치·정형 목록 질의를 위한 alias/FAQ/evidence locator 부족이 병목이었다. 상세 매트릭스와 개선 우선순위는 Infinity `wiki-retrieval-test-20q-01` artifact에 남겼다.

## 2026-09-01 20:20 UTC | 태스크 지식 흔적과 전체 승격 흐름 분리

- query: 태스크 상세에 재검증 상태를 강하게 결합하지 않고, 요청·조회 경로·남긴 로그만 남기며 전체 지식 흐름에서 별도 추적해 달라는 요청.
- 검색어: `origin_loop_id`, `source_context_pack`, `ingest manifest`, `source_file`, `knowledge promotion`.
- 읽은 문서: [[log|운영 로그]], `DOCUMENT_SEARCH_PIPELINE.md`, `INFINITY_OPERATING_RULES.md`, `ingest/INDEX.md`, `ingest/manifest.jsonl`, [[insights/bounded-experiment-loop|경계가 있는 반복 개선 루프]].
- raw KL: `ingest/INDEX.md`와 `manifest.jsonl`을 확인했다. `selected`·`integrated` 상태와 Wiki 출처/target이 명시된 연결만 사용했고, 상태나 문장 유사도로 승격을 추정하지 않았다.
- 결과: Infinity 태스크 상세은 `요청 쿼리 → 조회한 경로(Context Map) → 남긴 로그`로 구성하고, 기존 재검증 상태는 전체 `지식 흐름` 화면으로 이동한다. 별도 승격 목록은 `소스 → Ingest → Wiki 승격`을 상태·원본·명시 Wiki 링크 기준으로 표시한다.

## 2026-09-01 19:30 UTC | OpenClaw source ingest와 버전 트래커 원칙 갱신

- ingest: `source/openclaw-system`의 최근 변경분을 점검했고, `version_trackers.yaml`의 최신 버전 판단과 `LOCAL_REVIEW_AUTOMATION.md`의 현재성 게이트가 재사용 가치가 있는지 확인했다.
- query: `version tracker`, `review routing`, `currentness`, `daily-tracking`, `monthly review`를 기준으로 기존 위키와 중복 여부를 검사했다.
- 결과: `maintenance/version-tracker-refresh`를 최신값 정규화와 patch/minor/major 판정 분리 중심으로 갱신했다. 일일 리뷰의 소스 결합과 현재성 규칙은 이미 `review-routing-currentness`에 충분히 정리돼 있어 중복 승격은 보류했다.

## 2026-09-01 17:37 UTC | 지식 피드백 루프 가시성 점검

- query: ingest·query·lint 로그를 바탕으로 재검색·재판단이 실제로 이어지는지 점검하고, 사용자 가시성을 Infinity에 둘지 검토.
- 검색어: `ingest`, `query log`, `feedback loop`, `knowledge promotion`, `agent-wiki`.
- 읽은 문서: [[log|운영 로그]], `DOCUMENT_SEARCH_PIPELINE.md`, `schema/agent-rules.md`, `INFINITY_OPERATING_RULES.md`, `infinity/INTENTS.md`, Infinity 대시보드 렌더 경로.
- raw KL: 사용하지 않음. 현재 판단은 컴파일 로그와 Infinity 운영 원장·대시보드 코드의 연결 상태에 근거함.
- 결과: 로그 기록·지식 승격은 규칙으로 존재하지만, query 로그가 후속 재검색 후보로 다시 집계·실행되는 트리거는 없다. Agent Wiki는 근거 원장으로 두고, Infinity에 `지식 루프` 운영 보드를 추가해 최근 ingest/query/lint, 미해결 query, 재검색 대기, 승격 결과를 보여주는 방향이 적합하다.

## 2026-09-01 17:26 UTC | 구조 통합 후 참조 정리

- query: Agent Wiki와 Knowledge Lab에 남은 중복·죽은 참조를 정리해 달라는 요청.
- 검색어: `concepts`, `syntheses`, `sources`, `AGENT_RULES`, `KNOWLEDGE_LAB_README`.
- 읽은 문서: [[index|중앙 목차]], [[mapped/README|원본 카테고리 지도]], [[log|운영 로그]], `context-routes.json`, `ingest/INDEX.md`.
- raw KL: 외부 링크와 Infinity archive의 `integrated_into`·`knowledge_targets` 메타데이터를 확인했으며 모두 `integrated` 상태의 참조였다.
- 결과: 현재 경로는 `insights/`·`mapped/`·`content/docs/`로 통일하고, 공개 URL 호환용 legacy redirect와 과거 월별 로그는 보존하기로 했다.

## 2026-09-01 14:40 UTC | 중앙 목차 우선 탐색 구조

- 갱신 범위: 첫 화면을 27개 논지의 긴 나열에서 `중앙 목차 → 개념/종합 → 원본 대응 → 원문 확인` 흐름으로 재구성했다.
- 정리: 읽기 목적별 5개 진입점과 문서 타입별 역할을 명시해, 사이드바 분류를 이해하지 않아도 첫 화면에서 목적지로 이동하게 했다. 기존 논지 묶음은 근거 아카이브로 보존했다.
- 드리프트 점검: 원본 노트와 mapped 1:1 구조는 변경하지 않았고, 탐색 인터페이스만 정비했다.
